@model IEnumerable<Too.Models.CANTIDADPRODUCTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRODUCTO.NOMBREPROD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STOCKPROD)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRODUCTO.NOMBREPROD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STOCKPROD)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCANTIDADPROD }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCANTIDADPROD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCANTIDADPROD })
        </td>
    </tr>
}

</table>
