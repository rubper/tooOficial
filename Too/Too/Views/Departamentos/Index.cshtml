@model IEnumerable<Too.Models.DEPARTAMENTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRECATEGORIA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRECATEGORIA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDCATEGORIAPROD }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDCATEGORIAPROD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDCATEGORIAPROD })
        </td>
    </tr>
}

</table>
